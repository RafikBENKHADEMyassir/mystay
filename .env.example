# NOTE
# Next apps load env files from their own folders:
# - `frontend/.env.example` → `frontend/.env`
# - `admin/.env.example` → `admin/.env`
# Backend reads env vars from the shell (see `backend/.env.example`).

# Legacy (kept for reference)
# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Database / Auth (set actual values per env)
DATABASE_URL=postgresql://user:password@localhost:5432/mystay
AUTH_SECRET=replace-with-random-string

# PMS Connectors (examples)
PMS_PROVIDER=opera
PMS_BASE_URL=https://pms.example.com
PMS_CLIENT_ID=your-client-id
PMS_CLIENT_SECRET=your-client-secret

# Notifications
NOTIFY_WEBHOOK_SECRET=replace-with-secret
