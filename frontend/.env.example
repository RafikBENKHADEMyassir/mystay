# Guest PWA (frontend)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Backend API (server-side, used by Next.js API routes to reach the backend)
BACKEND_URL=http://localhost:4000

# Backend API (client-side, inlined at build time — only needed if browser calls backend directly)
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Admin dashboard (dev convenience)
NEXT_PUBLIC_ADMIN_URL=http://localhost:3001

# A strong, unique secret for signing session cookies.
# You can generate one with: openssl rand -base64 32
SESSION_SECRET=""

# Google Maps API key (for transport booking map).
# Get one at https://console.cloud.google.com/apis/credentials
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=""
